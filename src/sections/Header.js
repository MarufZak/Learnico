import {Navbar,Advertisement} from "../components";

const Header = () => {
  return (
    <header className="header">
      <Advertisement />
      <Navbar />
    </header>
  );
};

export default Header;
